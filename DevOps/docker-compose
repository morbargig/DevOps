version: '3.5'

services:

    jenkins:
        image: jeck
        container_name: jenkinsx
        volumes: 
            - jenkins-data-xy:/var/jenkins_home
            - /var/run/docker.sock:/var/run/docker.sock
            
        ports: 
            - 8080:8080

    Weight-app:
        build: .
        volumes: 

        ports:



            
    
    Weight-db:
        image:
        volumes: 

        ports:
        